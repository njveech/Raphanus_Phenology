# Coding agents

**See [NOTES.md](NOTES.md) for repository structure**, conventions-by-layout, and what is tracked vs. ignored. Treat **NOTES.md** as the canonical map before changing scripts or paths.

**Stack (brief):** R and Quarto for phenology analyses; Python for utilities (JSON, images, CSV rebuilds). Expect **large data directories** (bulk JSON/XML) and paths excluded by `.gitignore` — details are in **NOTES.md**.
